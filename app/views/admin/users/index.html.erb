<% provide :title, t("admin.user.all_user") %>
<div class="row">
  <div class="box">
    <div class="col-lg-12">
      <hr>
      <h2 class="intro-text text-center"><strong><%= t "admin.user.all_user" %></strong></h2>
      <hr>
      <ul class="col-lg-12">
        <div class="col-md-offset-9"><%= will_paginate @users %></div>
          <%= render partial: "user", collection: @users %>
        <div class="col-md-offset-9"><%= will_paginate @users %></div>
      </ul>
    </div>
  </div>
</div>