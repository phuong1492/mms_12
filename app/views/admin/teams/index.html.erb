<% provide :title, t("admin.team.all_team") %>
<div class="row">
  <div class="box">
    <div class="col-lg-12">
      <hr>
      <h2 class="intro-text text-center"><strong><%= t "admin.team.all_team" %></strong></h2>
      <hr>
      <ul class="col-lg-12">
        <div class="col-md-offset-9"><%= will_paginate %></div>
          <%= render @teams %>
        <div class="col-md-offset-9"><%= will_paginate %></div>
      </ul>
    </div>
  </div>
</div>
