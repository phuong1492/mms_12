<% provide :title, t("admin.position.all_position") %>
<div class="row">
  <div class="box">
    <div class="col-lg-12">
      <hr>
      <h2 class="intro-text text-center">
        <strong><%= t "admin.position.all_position" %></strong>
      </h2>
      <hr>
      <div><%= link_to t("admin.position.create"), new_admin_position_path,
        class: "edit-btn btn btn-primary col-md-4 col-md-offset-4" %></div>
      <ul class="col-lg-12">
        <div class="col-md-offset-9"><%= will_paginate %></div>
        <%= render partial: "position", collection: @positions %>
        <div class="col-md-offset-9"><%= will_paginate %></div>
      </ul>
    </div>
  </div>
</div>
