<% provide :title, t("project.index") %>
<div class="row">
  <div class="box">
    <div class="col-lg-12">
      <hr>
      <h2 class="intro-text text-center">
        <strong><%= t "project.index" %></strong>
      </h2>
      <hr>
      <ul class="col-lg-12">
        <%= render partial: "project", collection: @projects %>
        <div class="col-md-offset-9"><%= will_paginate %></div>
      </ul>
    </div>
  </div>
</div>
